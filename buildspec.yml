version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - sh gradlew clean assemble
artifacts:
  files:
    - appspec.yml
    - build/libs/cloud-computing-aws-1.0-SNAPSHOT.jar
    - start_application.sh
  discard-paths: yes